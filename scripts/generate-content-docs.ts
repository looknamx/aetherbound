import { mkdir, writeFile } from 'node:fs/promises';
import { UNITS, ITEMS, TRAITS, ODDS, RARITIES } from '../shared/content';
await mkdir('docs',{recursive:true});
const lines=['# Content catalog v1','','Generated from `shared/content.ts`. Run `npm run docs:content` after balance edits.','','## Recruits','','| ID | Name | Rarity / cost | Origin / class | HP | ATK | AS | Range | Armor / MR | Mana | Move | Target | Skill |','|---|---|---|---|---:|---:|---:|---:|---|---|---:|---|---|',...UNITS.map(u=>`| ${u.id} | ${u.name} | ${RARITIES[u.cost-1]} / ${u.cost} | ${u.origin} / ${u.class} | ${u.hp} | ${u.attack} | ${u.speed} | ${u.range} | ${u.armor} / ${u.resist} | ${u.mana} / ${u.maxMana} | ${u.moveSpeed} | ${u.targeting} | ${u.skill.name} (${u.skill.power}) |`),'','2-star HP/ATK/spell power ×1.8. 3-star ×3.24. Other base stats unchanged.','','## Skills','',...UNITS.map(u=>`- **${u.skill.name}** — ${u.skill.description} Base power: ${u.skill.power}.`),'','## Traits','','Each is active at 2 / 4 unique deployed recruits.','',...TRAITS.map(t=>`- **${t.id}:** ${t.description}`),'','## Relics','','| ID | Name | Effect |','|---|---|---|',...ITEMS.map(i=>`| ${i.id} | ${i.name} | ${i.description} |`),'','## Shop rarity odds (%)','','| Level | Common | Uncommon | Rare | Epic | Legendary |','|---|---:|---:|---:|---:|---:|',...ODDS.map((o,i)=>`| ${i+1} | ${o.join(' | ')} |`),''];
await writeFile('docs/CONTENT.md',lines.join('\n'));
